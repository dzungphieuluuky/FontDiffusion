import pytest
import torch

@pytest.fixture
def sample_tensor():
    return torch.randn(2, 128, 640)

@pytest.fixture
def sample_tensor_variable_config():
    return torch.randn(2, 64, 320)

@pytest.fixture
def sample_tensor_shape():
    return torch.randn(1, 128, 640)

@pytest.fixture
def mock_model():
    class MockModel:
        def __init__(self):
            self.config = {'input_shape': (128, 640)}

        def forward(self, x):
            return x

    return MockModel()